apply plugin: 'org.springframework.boot'

dependencies {
    compile project(":warehouse-api")
    compile project(":warranty-api")
}

bootJar {
    archiveFileName = "warehouse-service.jar"
}

springBoot {
    buildInfo()
}