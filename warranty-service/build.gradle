apply plugin: 'org.springframework.boot'

dependencies {
    compile project(":common")
    compile project(":order-api")
    compile project(":warranty-api")
}

bootJar {
    archiveFileName = "warranty-service.jar"
}

springBoot {
    buildInfo()
}